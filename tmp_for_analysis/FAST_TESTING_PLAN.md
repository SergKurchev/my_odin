# 🚀 БЫСТРЫЙ ПЛАН: Тестирование за 1 час вместо 6

**Обновлено:** 28 апреля 2026, 16:29  
**Стратегия:** Быстрые итеративные тесты по 1 часу

---

## 🎯 НОВАЯ СТРАТЕГИЯ

### Почему 1 час достаточно?

Из анализа OLD VERSION метрик мы знаем траекторию обучения:

| Итерация | Время (~) | OLD PQ | OLD mAP@50 | Статус |
|----------|-----------|--------|------------|--------|
| 143 | ~10 мин | 0.03 | 0.00 | Начало |
| 287 | ~20 мин | 0.18 | 0.10 | Разогрев |
| 575 | ~40 мин | 5.03 | 3.61 | **Критическая точка** |
| 863 | ~60 мин | 23.11 | 30.33 | **1 час - достаточно!** |
| 1151 | ~80 мин | 30.59 | 38.81 | Подтверждение |

**Вывод:** На итерации **863 (~1 час)** уже видно:
- OLD: PQ=23.11
- PRETRAIN: PQ=0.10 (деградация **-23.01**)

**Критерий успеха для 1-часового теста:**
- ✅ PQ ≥ 20 на итерации ~863
- ✅ mAP@50 ≥ 25 на итерации ~863

Если метрики ниже → что-то сломано, не нужно ждать 6 часов!

---

## ⚡ ОБНОВЛЕННЫЙ ПЛАН ДЕЙСТВИЙ

### Фаза 1: Создание Stable Branch (10 минут)

```bash
cd odin
git checkout b13865b
git checkout -b stable-strawberry-v1

# Cherry-pick критичные фиксы
git cherry-pick 3edd802  # Auto-sync Kaggle outputs
git cherry-pick 78f0739  # Fix NaN crash (LR=5e-5) ⚠️
git cherry-pick 7c73a54  # Fix speed calculation
git cherry-pick 46f0520  # Auto-calculate SOLVER.STEPS
git cherry-pick c3ca406  # Revert NUM_OBJECT_QUERIES
git cherry-pick b229c31  # Fix disk overflow 🔥
git cherry-pick 4a69382  # Fix TimeLimitHook
git cherry-pick 69f2f78  # Fix argparse conflict
git cherry-pick db3a8c7  # Fix double indexing
git cherry-pick c581f97  # Fix visualization 🔥
git cherry-pick e9772b8  # Fix CSV logging

git push origin stable-strawberry-v1
```

---

### Фаза 2: Быстрый Тест 1 (1 час обучения)

**Параметры для 1-часового теста:**

```python
train_cmd = [
    VENV_PYTHON, "my_odin/my_train_odin.py",
    "--config-file", CONFIG_FILE,
    "--num-gpus", "1",
    "--dist-url", "tcp://127.0.0.1:23456",
    "--resume",
    "--visualize",
    "--dataset_dir", DATASET_DIR,
    "--splits_file", SPLITS_FILE,
    
    # БЫСТРЫЙ ТЕСТ: 1 час
    "--num_epochs", "3",           # Вместо 10
    "--eval_period", "144",        # Каждую эпоху (~10 минут)
    "--checkpoint_period", "144",
    "--max_time", "1.2",           # 1.2 часа (с запасом)
    
    # Параметры обучения
    "--image_size", "640",
    "--batch_size", "1",
    "--num_frames", "5",
    "--lr", "0.0001",              # ВЕРНУТЬ 1e-4 (было 5e-5)
    
    # Config overrides
    'MODEL.WEIGHTS', CONFIG["ODIN_WEIGHTS_PATH"],
    'OUTPUT_DIR', './output',
    'SOLVER.AMP.ENABLED', 'True',
    
    # БЕЗ SWAG!
    # 'MODEL.BAYESIAN_TYPE', 'none',
]
```

**Ожидаемые результаты через 1 час:**

| Итерация | Ожидаемый PQ | Ожидаемый mAP@50 | Статус |
|----------|--------------|------------------|--------|
| 143 | ~0.03 | ~0.00 | Начало |
| 287 | ~0.18 | ~0.10 | Разогрев |
| 431 | ~1.10 | ~0.15 | Рост |
| 575 | ~5.03 | ~3.61 | Критическая точка |
| 719 | ~10.02 | ~10.97 | Хороший знак |
| 863 | **~23.11** | **~30.33** | **SUCCESS!** ✅

**Критерии успеха:**
- ✅ PQ ≥ 20 на итерации 863
- ✅ mAP@50 ≥ 25 на итерации 863
- ✅ Траектория роста похожа на OLD

**Критерии провала:**
- ❌ PQ < 5 на итерации 863 (как в PRETRAIN)
- ❌ Метрики не растут

---

### Фаза 3: Итеративная Отладка (если Тест 1 провалился)

**Если PQ < 20 на итерации 863:**

#### Тест 1.1: Откатить LR change (1 час)

```bash
git revert 78f0739  # Откатить LR=5e-5
```

```python
"--lr", "0.0001",  # Явно указать 1e-4
```

**Запустить тест на 1 час.**

**Ожидание:** PQ ≥ 20 на итерации 863

---

#### Тест 1.2: Откатить CAMERA_DROP (если был применен) (1 час)

```bash
git revert 3d0ea94  # Если был применен
```

**Запустить тест на 1 час.**

---

#### Тест 1.3: Минимальный baseline (1 час)

```bash
# Откатиться на чистый b13865b
git checkout b13865b
git checkout -b stable-minimal

# Применить ТОЛЬКО критичные фиксы
git cherry-pick b229c31  # Disk cleanup
git cherry-pick c581f97  # Visualization fix
```

**Запустить тест на 1 час.**

**Ожидание:** PQ ≥ 20 (должно работать, это почти чистый OLD)

---

### Фаза 4: Bayesian Inference (после успеха Теста 1)

**Создать feature branch:**

```bash
git checkout stable-strawberry-v1  # Или stable-minimal
git checkout -b feature/bayesian-inference-v2
```

#### Тест 2.1: Детерминированный baseline (1 час)

**Добавить BayesianInferenceWrapper (без активации):**

```python
# В my_train_odin.py
# Добавить wrapper класс, но использовать method='none'
```

**Запустить тест на 1 час.**

**Ожидание:** PQ ≥ 20 (идентично Тесту 1)

---

#### Тест 2.2: MC Dropout (1 час обучения + 10 минут eval)

**Обучение (детерминированное):**
```python
"--num_epochs", "3",
"--max_time", "1.2",
# БЕЗ Bayesian inference во время обучения
```

**Eval (с MC Dropout):**
```python
eval_cmd = [
    VENV_PYTHON, "my_odin/my_train_odin.py",
    "--config-file", CONFIG_FILE,
    "--num-gpus", "1",
    "--eval-only",
    "--dataset_dir", DATASET_DIR,
    "--splits_file", SPLITS_FILE,
    
    'MODEL.WEIGHTS', './output/model_final.pth',
    'MODEL.BAYESIAN_TYPE', 'mc_dropout',
    'MODEL.BAYESIAN_SAMPLES', '10',
    'MODEL.MASK_FORMER.DROPOUT', '0.1',
    'OUTPUT_DIR', './output',
]
```

**Ожидание:**
- PQ ≥ 18 (допустимо небольшое снижение)
- **Uncertainty > 0** (главное!)

---

#### Тест 2.3: SWAG (3 часа обучения + 10 минут eval)

**Обучение с SWAG collection:**
```python
"--num_epochs", "10",  # Нужно больше для SWAG
"--max_time", "3.5",
'MODEL.BAYESIAN_TYPE', 'swag',
'MODEL.SWAG.START_EPOCH', '5',
'MODEL.SWAG.UPDATE_FREQ', '5',
```

**Eval (с SWAG sampling):**
```python
'MODEL.BAYESIAN_TYPE', 'swag',
'MODEL.BAYESIAN_SAMPLES', '10',
```

**Ожидание:**
- PQ ≥ 18
- **Uncertainty > 0**

---

## 📊 ТАБЛИЦА ТЕСТОВ

| Тест | Время | Цель | Критерий успеха | Действие при провале |
|------|-------|------|-----------------|---------------------|
| **1.0** | 1ч | Stable baseline | PQ ≥ 20 @ iter 863 | → Тест 1.1 |
| **1.1** | 1ч | Откат LR | PQ ≥ 20 @ iter 863 | → Тест 1.2 |
| **1.2** | 1ч | Откат CAMERA_DROP | PQ ≥ 20 @ iter 863 | → Тест 1.3 |
| **1.3** | 1ч | Минимальный baseline | PQ ≥ 20 @ iter 863 | Изучить логи |
| **2.1** | 1ч | Wrapper без активации | PQ ≥ 20 @ iter 863 | Фикс wrapper |
| **2.2** | 1ч+10м | MC Dropout | PQ ≥ 18, Unc > 0 | Фикс MC Dropout |
| **2.3** | 3ч+10м | SWAG | PQ ≥ 18, Unc > 0 | Фикс SWAG |

**Общее время тестирования:** ~8-10 часов (вместо 42 часов при 6-часовых тестах!)

---

## 🚀 ROADMAP С БЫСТРЫМИ ТЕСТАМИ

### День 1 (28 апреля, сегодня)
- [x] Расследование завершено
- [ ] Создать stable-strawberry-v1 branch (10 мин)
- [ ] Запустить Тест 1.0 на Kaggle (1 час)
- [ ] Проверить результаты (5 мин)

**Если успех:** → День 2  
**Если провал:** → Тест 1.1 (еще 1 час)

---

### День 2 (29 апреля)
- [ ] Реализовать BayesianInferenceWrapper (2 часа)
- [ ] Запустить Тест 2.1 (1 час)
- [ ] Проверить результаты (5 мин)

**Если успех:** → День 3  
**Если провал:** → Фикс wrapper (1 час)

---

### День 3 (30 апреля)
- [ ] Запустить Тест 2.2 MC Dropout (1ч + 10м)
- [ ] Проверить Uncertainty > 0
- [ ] Если успех → начать Тест 2.3 SWAG (3ч)

---

### День 4 (1 мая)
- [ ] Завершить Тест 2.3 SWAG
- [ ] Проверить результаты
- [ ] Если успех → финальное 6-часовое обучение для подтверждения

---

### День 5 (2 мая)
- [ ] Финальное 6-часовое обучение
- [ ] Подтверждение PQ ≥ 50
- [ ] Документация

---

## 📈 ИНТЕРПРЕТАЦИЯ РЕЗУЛЬТАТОВ

### Хорошие признаки (на итерации 863):

| Метрика | Значение | Интерпретация |
|---------|----------|---------------|
| PQ | ≥ 20 | ✅ Обучение работает |
| PQ | 15-20 | ⚠️ Работает, но медленнее |
| PQ | 10-15 | ⚠️ Что-то не так, но не критично |
| PQ | < 10 | ❌ Сломано |

### Траектория роста:

**Здоровая траектория (как OLD):**
```
Iter 143:  PQ ~0.03
Iter 287:  PQ ~0.18  (рост x6)
Iter 431:  PQ ~1.10  (рост x6)
Iter 575:  PQ ~5.03  (рост x4.5)
Iter 719:  PQ ~10.02 (рост x2)
Iter 863:  PQ ~23.11 (рост x2.3)
```

**Сломанная траектория (как PRETRAIN):**
```
Iter 143:  PQ ~0.00
Iter 287:  PQ ~0.00  (нет роста!)
Iter 431:  PQ ~0.00  (нет роста!)
Iter 575:  PQ ~0.08  (очень медленно)
Iter 719:  PQ ~0.00  (откат!)
Iter 863:  PQ ~0.10  (почти нет роста)
```

**Если видишь сломанную траекторию → немедленно останавливай тест!**

---

## 🎯 КРИТЕРИИ ПРИНЯТИЯ РЕШЕНИЙ

### После Теста 1.0 (1 час):

**Если PQ ≥ 20:**
- ✅ SUCCESS! Переходим к Фазе 4 (Bayesian Inference)

**Если 15 ≤ PQ < 20:**
- ⚠️ Работает, но медленнее. Запустить еще на 1 час до итерации 1151
- Ожидание: PQ ≥ 30 на итерации 1151

**Если 10 ≤ PQ < 15:**
- ⚠️ Что-то не так. Запустить Тест 1.1 (откат LR)

**Если PQ < 10:**
- ❌ Сломано. Запустить Тест 1.1 (откат LR)

---

### После Теста 1.1 (откат LR):

**Если PQ ≥ 20:**
- ✅ Проблема была в LR! Используем LR=1e-4

**Если PQ < 20:**
- ❌ Проблема не в LR. Запустить Тест 1.3 (минимальный baseline)

---

### После Теста 1.3 (минимальный baseline):

**Если PQ ≥ 20:**
- ✅ Минимальный baseline работает
- Добавлять коммиты по одному и тестировать

**Если PQ < 20:**
- ❌ Даже минимальный baseline не работает
- Проверить:
  1. Правильный ли коммит b13865b?
  2. Правильные ли данные (датасет)?
  3. Правильные ли веса (ODIN_WEIGHTS_PATH)?

---

## 🔧 БЫСТРАЯ ДИАГНОСТИКА

### Проверка 1: Логи обучения

**Смотреть на:**
```
iteration: 143, total_loss: ~38-45 (нормально)
iteration: 287, total_loss: ~34-38 (снижается)
iteration: 575, total_loss: ~30-32 (продолжает снижаться)
```

**Если total_loss не снижается → проблема с оптимизацией!**

---

### Проверка 2: Eval метрики

**Смотреть на:**
```
Iter 143: PQ=0.00, mAP@50=0.00 (нормально, модель еще не обучена)
Iter 287: PQ > 0.10, mAP@50 > 0.05 (начинает учиться)
Iter 575: PQ > 3.00, mAP@50 > 2.00 (хороший знак)
Iter 863: PQ > 20.00, mAP@50 > 25.00 (SUCCESS!)
```

---

### Проверка 3: Disk usage

**Смотреть на:**
```
output/
├── model_0000143.pth  (должен удалиться)
├── model_0000287.pth  (должен удалиться)
├── model_0000719.pth  (остается)
└── model_0000863.pth  (остается)
```

**Если старые чекпоинты не удаляются → CheckpointCleanupHook не работает!**

---

## 📋 QUICK CHECKLIST

### Сегодня (28 апреля, 16:30-18:00)
- [ ] Создать stable-strawberry-v1 branch (10 мин)
- [ ] Обновить kaggle_my_train_odin.py (5 мин)
- [ ] Запустить Тест 1.0 на Kaggle (1 час)
- [ ] Проверить метрики на итерации 863 (5 мин)

### Сегодня вечером (18:00-19:00)
- [ ] Если PQ ≥ 20 → SUCCESS! 🎉
- [ ] Если PQ < 20 → Запустить Тест 1.1 (1 час)

### Завтра утром (29 апреля, 9:00)
- [ ] Проверить результаты Теста 1.1
- [ ] Если успех → начать Фазу 4 (Bayesian Inference)

---

## 🎓 КЛЮЧЕВЫЕ ИЗМЕНЕНИЯ

### Было (старый план):
- ❌ Тесты по 6 часов
- ❌ Ждать до конца для оценки
- ❌ Долгая итерация (42 часа на все тесты)

### Стало (новый план):
- ✅ Тесты по 1 часу
- ✅ Оценка на итерации 863
- ✅ Быстрая итерация (8-10 часов на все тесты)

**Экономия времени: 32 часа!**

---

**Начинай прямо сейчас! Первый тест займет всего 1 час! 🚀**
